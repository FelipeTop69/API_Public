import { Routes } from '@angular/router';
import { IndiceUserComponent } from './Views/indice-user/indice-user.component';

export const routes: Routes = [
    {path: 'user', component: IndiceUserComponent}
];
